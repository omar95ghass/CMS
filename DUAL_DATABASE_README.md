# نظام قاعدة البيانات المزدوجة مع Failover

## نظرة عامة

تم تطوير نظام قاعدة بيانات مزدوجة يدعم MySQL و SQLite مع آلية failover تلقائية ومزامنة بين قاعدتي البيانات.

## المميزات الجديدة

### 1. نظام قاعدة البيانات المزدوجة
- **MySQL**: قاعدة البيانات الأساسية
- **SQLite**: قاعدة البيانات الاحتياطية
- **Failover تلقائي**: عند فشل MySQL، ينتقل النظام تلقائياً إلى SQLite
- **مزامنة تلقائية**: مزامنة البيانات بين قاعدتي البيانات

### 2. إصلاح نظام النداء المشترك
- عند نداء دور مشترك، يتم وضع `called` على الدور الذي يخص الشباك الذي قام بالنداء
- يتم وضع `completed` على باقي الأدوار بنفس الرقم والخدمة

### 3. واجهة إدارة قواعد البيانات
- عرض حالة الاتصال بقاعدتي البيانات
- إحصائيات مفصلة لكل قاعدة بيانات
- أدوات المزامنة اليدوية
- إصلاح عدم التطابق في البيانات

### 4. إدارة صورة اللوغو
- رفع وتحديث صورة اللوغو من خلال واجهة الإدارة
- دعم تلقائي لصيغ الصور المختلفة (PNG, JPG, GIF, WebP)
- تغيير حجم الصور تلقائياً
- إنشاء نسخ احتياطية من اللوغو السابق

## الملفات الجديدة

### ملفات قاعدة البيانات
- `php/dual_db.php` - نظام قاعدة البيانات المزدوجة
- `php/sync_manager.php` - مدير المزامنة
- `php/cron_sync.php` - مزامنة تلقائية
- `database/` - مجلد قاعدة بيانات SQLite

### ملفات الواجهة
- `php/upload_logo.php` - رفع صورة اللوغو
- تحديثات على `settings.php` - واجهة إدارة قواعد البيانات

### ملفات الاختبار
- `test_dual_db.php` - اختبار نظام قاعدة البيانات المزدوجة

## التثبيت والإعداد

### 1. إنشاء مجلد قاعدة البيانات
```bash
mkdir -p /path/to/project/database
chmod 755 /path/to/project/database
```

### 2. إعداد صلاحيات الملفات
```bash
chmod 755 /path/to/project/images/logo
chmod 644 /path/to/project/logs/db_errors.log
```

### 3. إعداد Cron Jobs
```bash
# مزامنة تلقائية كل 5 دقائق
*/5 * * * * /usr/bin/php /path/to/project/php/cron_sync.php >> /var/log/queue_sync.log 2>&1

# تنظيف يومي في الساعة 6:00 صباحاً
0 6 * * * /usr/bin/php /path/to/project/php/daily_cleanup.php >> /var/log/queue_cleanup.log 2>&1
```

## الاستخدام

### 1. اختبار النظام
```bash
php test_dual_db.php
```

### 2. إدارة قواعد البيانات
- انتقل إلى `settings.php`
- اختر قسم "حالة قواعد البيانات"
- راقب حالة الاتصال والمزامنة
- استخدم أدوات المزامنة اليدوية عند الحاجة

### 3. إدارة اللوغو
- انتقل إلى `settings.php`
- في قسم "إعدادات النظام"
- استخدم أداة رفع اللوغو
- اختر صورة جديدة واحفظها

## API Endpoints

### مزامنة قواعد البيانات
```
GET php/sync_manager.php?action=sync
GET php/sync_manager.php?action=status
GET php/sync_manager.php?action=statistics
GET php/sync_manager.php?action=repair
```

### رفع اللوغو
```
POST php/upload_logo.php
Content-Type: multipart/form-data
```

## مراقبة النظام

### 1. سجلات قاعدة البيانات
```bash
tail -f logs/db_errors.log
```

### 2. سجلات المزامنة
```bash
tail -f /var/log/queue_sync.log
```

### 3. سجلات التنظيف
```bash
tail -f /var/log/queue_cleanup.log
```

## استكشاف الأخطاء

### 1. مشاكل الاتصال
- تحقق من إعدادات MySQL
- تأكد من صلاحيات مجلد قاعدة البيانات
- راجع سجلات الأخطاء

### 2. مشاكل المزامنة
- تحقق من توفر كلا قاعدتي البيانات
- استخدم أداة الإصلاح في واجهة الإدارة
- راجع سجلات المزامنة

### 3. مشاكل رفع اللوغو
- تحقق من صلاحيات مجلد الصور
- تأكد من صيغة الملف المدعومة
- راجع حجم الملف (الحد الأقصى 2MB)

## الأمان

- جميع الملفات المحملة يتم التحقق من نوعها
- حجم الملفات محدود بـ 2MB
- الصور يتم تحويلها إلى PNG للتوافق
- إنشاء نسخ احتياطية من اللوغو السابق

## الدعم

للحصول على الدعم أو الإبلاغ عن مشاكل:
1. راجع سجلات النظام
2. استخدم أداة الاختبار `test_dual_db.php`
3. تحقق من حالة قواعد البيانات في واجهة الإدارة

## التحديثات المستقبلية

- دعم قواعد بيانات إضافية
- مزامنة متقدمة مع حل النزاعات
- واجهة مراقبة متقدمة
- إشعارات تلقائية عند فشل الاتصال